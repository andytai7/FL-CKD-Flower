# REGENERATE — timeseries track results index

Every file in `results/` is a local, gitignored artifact. This index maps each to the
exact regenerator command (run from the repo root on `experiment/timeseries`; `uv run
--extra dl python -m research.privacy_dl_ts.<module>`). Runtime estimates are measured on
this box (OMP_NUM_THREADS=4). Run the matrix emitter LAST, after its inputs exist.

| Artifact | Regenerator (module) | Runtime (measured) |
|---|---|---|
| `dl_ts_p1_adam.json` (+`_transport`, `_seeds`) — ACTIVE P1 grid/arms | `p1_rerun` | ~5 min/cell; full rerun ~2.5 h (seed-42 grid + ε=1 transports + seeds 43-46 subset) |
| `dl_ts_p1.json` (+`_seeds`) — pass-1 SGD, SUPERSEDED (retained as the audit record) | `p1_spine`, `sweep_p1_seeds` | legacy — do not regenerate unless re-auditing |
| `dl_ts_p2.json` — analytic protocol cost table | `p2_secagg` | seconds |
| `dl_ts_p2.json` measured_row | NOT regenerated by a module — evidence appended from the `p2fab/` deployment probe (SuperLink + 10 SuperNodes run; stage-1 wedge record) | manual |
| `dl_ts_p3.json` — FedCT Gaussian-vote grid | `fedct_ts` | ~1 h (teachers per seed/ε) |
| `dl_ts_p4.json` — verified-hybrid grid + proofs | `p4_spine` | ~1 h |
| `dl_ts_forecast_grid.json` — GRU/LSTM crosscheck | `forecast` (resume-safe) | hours for h192/336 decoder rollouts; h96 columns minutes |
| same file, `model=linear` columns | `forecast_linear_grid` (appends; resume-safe) | ~15 min total |
| `dl_ts_forecast_p1.json` — event pilot + GRU user-level | `forecast_p1` | event pilot ~25 min/round (honest loops); user-level cells minutes |
| `dl_ts_forecast_p1_linear.json` — linear user-level feasibility | `forecast_p1_linear` (resume-safe) | minutes |
| `dl_ts_matrix.json` — THE schema-locked matrix | `matrix` | seconds |

Writer-discipline rule (learned 2026-09-04/05 the hard way on the image track): sweep
scripts own ONLY their `_seeds` file; never call a `run_grid` whose checkpoint path is the
grid file — call `run_cell` and manage the envelope yourself. TS `sweep_p1_seeds.py` is
written that way (writes the legacy `_seeds` envelope only); keep it that way.

Baseline artifacts (the audit.json family: audit/benchmark/equity/phasemap/privacy.json)
are NOT research-tree products — they regenerate via the deployable pipeline in the root
README / docs (logreg stack), not from this tree.
