"""Research tree for the dermoscopy DL × privacy benchmark (track-local rule-2 waiver).

Design contract: `../privacy-dl-image/README.md`. Nothing here is imported by the deployable
modules (`server_app.py`, `client_app.py` stay logreg); importing requires the `dl` uv extra.
"""
